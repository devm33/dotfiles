
# Show contents of directory after cd-ing into it
chpwd() {
  if [ -d .git ]; then
      git status
      echo '--'
  fi
  ls
}

# Hail vim
alias v='vim'
alias vi='vim'

alias vg='vagrant'

alias untar='tar xvf '

alias xo='xdg-open '
